#include<stdio.h>
#include<conio.h>
void main()
{
int n,i,#include<stdio.h>
#include<conio.h>
void main()
{
int n,i,a[90],sum=0;
printf("enter the no");
scanf("%d",&n);
printf("  array element");
for(i=0;i<n;i++)
scanf("%d",&a[i]);
for(i=0;i<n;i++)
sum=sum+a[i];
printf("%d",sum);
}a[90];
printf("enter the no");
scanf("%d",&n);
printf(arry  array element");
for(i=0;i<n;i++)
scanf("%d",&a[i]);
for